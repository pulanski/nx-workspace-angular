import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sortir',
  webDir: '../../../dist/apps/mobile/sortir',
  bundledWebRuntime: false,
};

export default config;
